pipeline { 
 agent any 
  stages { 
     stage('deploy') { 
        steps {
          sh 'kubectl apply -f weather-app-deployment.yaml'
          sh 'kubectl apply -f weather-app-service.yaml'
        }
     }
   }
}
